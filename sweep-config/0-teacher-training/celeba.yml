command:
  - ${env}
  - python
  - ${program}
  - ${args}
program: scripts/train-celeba-teacher.py
method: grid
metric:
  goal: minimize
  name: val_avg_auroc
parameters:
  arch:
    values: 
      - resnet18 
      - resnet50
      - vitb16
      - wideresnet50-2
  epochs:
    value: 50